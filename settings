# This file must be used with "source settings" *from bash*
# you cannot run it directly
export APP_SETTINGS="web.config.DevelopmentConfig"
export SECRET_KEY="not-so-secret"
export DATABASE_URL="postgresql://postgres@localhost/training_stats"
